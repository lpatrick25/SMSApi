import type { CapacitorConfig } from '@capacitor/cli';
import { CapacitorHttp } from '@capacitor/core';

const config: CapacitorConfig = {
  appId: 'com.capstone.smsapi',
  appName: 'SMS API',
  webDir: 'www'
};

export default config;
